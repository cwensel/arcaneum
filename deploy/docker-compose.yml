services:
  # Qdrant vector database (RDR-002)
  qdrant:
    # Keep the named volume identifiers below stable across image upgrades.
    image: qdrant/qdrant:v1.18.2
    container_name: qdrant-arcaneum
    restart: unless-stopped

    ports:
      - "6333:6333"  # REST API
      - "6334:6334"  # gRPC API

    volumes:
      - qdrant-arcaneum-storage:/qdrant/storage
      - qdrant-arcaneum-snapshots:/qdrant/snapshots

    environment:
      - QDRANT__LOG_LEVEL=INFO
      # Disk-biased configuration for low-memory operation
      - QDRANT__STORAGE__ON_DISK_PAYLOAD=true
      - QDRANT__STORAGE__WAL__WAL_CAPACITY_MB=16
      - QDRANT__STORAGE__OPTIMIZERS__DEFAULT_SEGMENT_NUMBER=2
      - QDRANT__STORAGE__OPTIMIZERS__MAX_SEGMENT_SIZE_KB=100000
      - QDRANT__STORAGE__OPTIMIZERS__FLUSH_INTERVAL_SEC=10
      - QDRANT__STORAGE__OPTIMIZERS__INDEXING_THRESHOLD_KB=15000

    deploy:
      resources:
        limits:
          memory: 4G
          cpus: '2.0'
        reservations:
          memory: 2G
          cpus: '1.0'

  # MeiliSearch full-text search (RDR-008)
  meilisearch:
    image: getmeili/meilisearch:v1.12
    container_name: meilisearch-arcaneum
    restart: unless-stopped

    ports:
      - "7700:7700"  # HTTP API

    volumes:
      - meilisearch-arcaneum-data:/meili_data
      - meilisearch-arcaneum-dumps:/dumps
      - meilisearch-arcaneum-snapshots:/snapshots

    environment:
      # Core settings (MEILI_* are MeiliSearch's internal env vars)
      # API key is auto-generated by 'arc container start' and stored in
      # ~/.config/arcaneum/meilisearch.key (or use MEILISEARCH_API_KEY env var)
      - MEILI_ENV=${MEILI_ENV:-production}
      - MEILI_MASTER_KEY=${MEILISEARCH_API_KEY}
      - MEILI_HTTP_ADDR=0.0.0.0:7700

      # Resource management (CRITICAL: Must set manually - MeiliSearch issue #4686)
      # Merge cost scales with total index size, so indexing a large index is
      # bound by these two values. Keep MEILI_MAX_INDEXING_MEMORY well under the
      # container limit below: it budgets the indexer only, and the process also
      # needs headroom for search and LMDB page cache.
      - MEILI_MAX_INDEXING_MEMORY=${MEILI_MAX_INDEXING_MEMORY:-8GiB}
      # Meilisearch's own guidance is to stay at or below half the available
      # cores so search latency does not suffer during indexing (12-core host).
      - MEILI_MAX_INDEXING_THREADS=${MEILI_MAX_INDEXING_THREADS:-6}

      # Payload limits
      - MEILI_HTTP_PAYLOAD_SIZE_LIMIT=100MB

      # Persistence paths
      - MEILI_DB_PATH=/meili_data/data.ms
      - MEILI_DUMP_DIR=/dumps
      - MEILI_SNAPSHOT_DIR=/snapshots

    deploy:
      resources:
        limits:
          memory: 12G
          cpus: '8.0'
        reservations:
          memory: 2G
          cpus: '2.0'

    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:7700/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

volumes:
  # Qdrant volumes. Do not rename these without an explicit data migration.
  qdrant-arcaneum-storage:
    driver: local
  qdrant-arcaneum-snapshots:
    driver: local
  # MeiliSearch volumes. Do not rename these without an explicit data migration.
  meilisearch-arcaneum-data:
    driver: local
  meilisearch-arcaneum-dumps:
    driver: local
  meilisearch-arcaneum-snapshots:
    driver: local
